<h1>ノート編集ページ</h1>

<%= form_with model: @note do |f| %>
  <p>
      <%= f.label :title, 'タイトル：' %>
      <%= f.text_field :title %>
  </p>
  <p>
      <%= f.label :body, '本文：' %>
      <%= f.rich_text_area :body %>
  </p>
  
  <%= f.submit '変更' %>
<% end %>
<%= link_to 'キャンセル', notes_path %>